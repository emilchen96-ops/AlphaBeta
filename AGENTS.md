# AlphaDesk 开发规则

## 项目目标

AlphaDesk 是一个网页端个人量化交易系统。它以模拟盘为起点，通过可审计的事件、订单状态机、双层风控和可靠消息链路，逐步演进到经人工确认的小额实盘；任何阶段均以资金安全和可追溯性优先。

## 任务前必读

1. 每次任务开始前，必须阅读 [docs/index.md](docs/index.md)。
2. 修改某一模块前，必须阅读索引中列出的对应领域文档及相关 ADR。
3. 不得绕过领域边界：策略只产生 Signal；订单必须经过风控和状态机；Broker 只能经 Adapter 调用。

## 开发与安全规则

1. 禁止在代码、测试、文档、前端、Git 仓库或普通配置文件中写入密钥、账户凭证或交易权限。
2. 每次修改必须补充或调整相应测试；不得删除既有功能来规避测试。
3. 每次修改必须同步更新相关文档、ADR 或接口契约。
4. 数据库结构修改必须使用 Migration；领域层不得直接依赖 FastAPI、Redis 或具体 Broker。
5. 未经明确要求，不得接入实盘；任何实盘能力须在模拟盘、只读接入和人工确认小额实盘之后评审启用。

## 交付要求

每次完成任务必须报告：修改的文件、文档更新、测试结果，以及未验证或待决风险。详细设计以 `docs/` 为准，本文件仅保留执行规则。
