# AlphaDesk Web (M01)

React + TypeScript local administration shell. The dashboard reads real infrastructure status from the M01 API; every trading-related route is an explicit unimplemented placeholder.

Only `VITE_`-prefixed, non-sensitive values may be exposed to this application. Authentication is not implemented, so this application must not be deployed publicly.
