# ADR 0001：MVP 采用模块化单体

- 状态：Accepted

## 背景

个人量化系统需要快速验证完整的审计、订单和风控闭环，同时控制部署、调试与一致性成本。

## 决策

MVP 使用一个按领域模块组织的后端单体；Windows 执行器保持独立进程。模块通过明确接口和事件协作，不预先拆分为大量微服务。

## 原因

降低分布式事务、可观测性、部署和运维复杂度，同时保留未来拆分所需的边界。

## 后果

需要严格维护模块依赖和接口；将来若负载或隔离需求证明必要，可基于既有边界拆分。

## 被否决方案

一开始采用多微服务；将所有功能（含执行器）置于单一无边界进程。
