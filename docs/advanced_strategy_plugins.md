# 高级策略插件

`AdvancedStrategyPlugin` 是受信任开发者扩展端口，不是在线代码执行器。插件在服务启动时
注册，提供键、中文名称、版本、参数 Schema、周期、所需行情字段和能力标记，并实现现有
`Strategy` 接口。

插件只能产生 `Signal`，不能调用 Broker、读取 Secret、执行系统命令或绕过风控。普通用户
不能上传 Python、安装依赖或指定任意实现路径。禁用插件不能运行。普通页面根据参数 Schema
生成通用表单，不为每个插件创建专用页面。
