# 选股历史结果

“历史结果”按方案名称、状态、来源和筛选日期查询已经创建的选股运行。列表显示方案、
筛选日期、股票范围、处理状态、匹配数量、数据不足数量、耗时与完成时间。

结果详情首先展示本次运行的不可变规则快照、筛选日期和范围，然后展示：

- 处理总量、完成量、数据不足或无法判断数量、入选数量；
- 股票名称与代码、参考价、筛选日涨跌、动态条件值、入选原因和数据提示；
- 零结果时最常见的淘汰条件；
- 部分失败时仍可使用的真实入选结果。

历史结果可以再次载入条件，但重新执行前必须由用户确认。原历史记录不会被修改。

