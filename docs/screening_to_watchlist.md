# 选股结果加入自选股

用户可以从选股结果选择一只或多只股票，加入现有自选分组、新建分组，或加入“盘中监控”。

批量操作最多处理 200 只。服务端只接受属于本次选股结果的标的，并返回：

- 成功加入数量；
- 已经存在数量；
- 失败数量。

重复提交不会创建重复成员。“盘中监控”表示该自选分组可以形成行情订阅意图，不代表
MiniQMT 已经实际订阅；行情页仍需分别显示期望订阅与实际连接状态。此流程不创建订单。

