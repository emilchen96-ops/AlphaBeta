# BT02-A 验收记录

验收日期：2026-08-03（Asia/Shanghai）

## 已通过

- 领域、应用服务、API 聚焦回归：57 项通过；
- 覆盖分钟闭合后确认、下一真实分钟执行、午休跨越、尾盘无下一分钟、安全预筛等价性、
  批量取消/失败重试 API 和延迟重试认领；
- React/TypeScript 工程编译通过；
- 前端聚焦回归通过：快速回测 2 项、回测详情与批量回测 7 项；
- 已验证旧回测记录缺少 BT02-A 新增摘要字段时仍可正常打开；
- 确定性性能基准完成，原始结果见 [性能基准](bt02a_benchmark.md)。

## 未完成的环境验收

- 当前 Docker Desktop daemon 未运行，不能执行 PostgreSQL Migration、Docker Worker 重启恢复和
  浏览器端到端验收；
- 当前验收窗口没有可确认的 MiniQMT 在线会话，未实测真实 A 股日线/分钟缺口下载速度；
- 前端测试存在 jsdom 无法解析 Ant Design 样式表的非阻断警告，不影响 9 项聚焦回归结果。

以上项目属于环境或测试运行器阻塞，不使用受控 Fixture 替代真实验收。Docker 和 MiniQMT 可用后
应补跑：Migration 升级、API/Worker 健康、单股一分钟触发、组合取消/重试、全 A 股小窗口、页面
刷新恢复，以及真实下载耗时记录。
