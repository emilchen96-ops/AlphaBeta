# BT02-A 验收记录

验收日期：2026-08-03（Asia/Shanghai）

## 自动化验收

- 领域、应用服务、API 聚焦回归通过；最终收尾聚焦集 49 项通过；
- 覆盖分钟闭合后确认、下一真实分钟执行、午休跨越、尾盘无下一分钟、安全预筛等价性、
  集合竞价差异兼容、分钟回放单调时钟、批量取消/失败重试和延迟重试认领；
- Ruff、Python `compileall`、React/TypeScript 编译和 Vite 生产构建通过；
- 前端聚焦回归通过：快速回测 2 项、回测详情与批量回测 7 项；
- 已验证旧回测记录缺少 BT02-A 新增摘要字段时仍可正常打开；
- 确定性性能基准完成，原始结果见 [性能基准](bt02a_benchmark.md)。

## Docker 与浏览器验收

- PostgreSQL、PostgreSQL Test、Redis、API、Web、行情 Worker、扫描 Worker、回放 Worker、
  批量回测 Worker 和 AI 调研 Worker 均处于健康状态；
- API `/health/ready` 返回 `ready`，PostgreSQL 与 Redis 均在线；
- 浏览器打开策略回测页成功，系统连接、API、PostgreSQL、Redis 和 WebSocket 均显示在线；
- BT02-A 页面默认展示“盘中分钟触发，下一根分钟开盘成交”，一分钟信号周期和自动补齐候选日
  分钟行情均可见。

## 真实 MiniQMT 验收

- 东莞证券 MiniQMT 进程与 AlphaDesk 只读 Agent 均在线；目录同步 7,209 个标的；
- 28/28 个行情订阅激活，失败 0；行情能力启用，交易能力保持关闭；
- 长信科技（300088）候选日分钟缺口由 MiniQMT 补齐，共 2 个交易日、480 根一分钟 K 线；
- MiniQMT 连续竞价分钟线不包含集合竞价，而日线包含集合竞价。系统现将安全的开盘价、极值和
  成交量单向差异记为质量警告；分钟价格越出日线包络、收盘价不一致或分钟成交量超过日线仍为
  致命错误；
- 真实回测 `9bddb601-5088-4d4b-8220-b8c7c540386b` 使用“10 日价格突破 + 1.2 倍成交量，
  5 日均线退出”，状态为 `COMPLETED`：13 个日线交易日、2 个候选日、2 个分钟回放日、
  480 根分钟 K 线、1 个信号、1 张模拟订单、1 笔模拟成交，致命质量问题 0；
- 本次真实小窗口总耗时约 0.90 秒，其中数据准备约 0.04 秒、策略回放约 0.86 秒；分钟数据已在
  本地时会被复用，不重复全量下载。

以上真实验收仅验证只读行情与历史模拟链路，不启用或测试券商下单。
